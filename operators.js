let earthDiameter = 12742; // represents Earth diameter, in kms
let marsDiameter = 6779; // represents Mars diameter, in kms
let venusDiameter = 12104; // represents Venus diameter, in kms

// TODO: Compare if Earth is larger than Mars AND larger than Venus, then log the result.
console.log(earthDiameter > marsDiameter && earthDiameter > venusDiameter);
